<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.koreait.koreaitsugang.repository.AccountRepository">

    <resultMap id="userMst" type="com.koreait.koreaitsugang.entity.UserMst">
        <result property="userId" column="user_id"></result>
        <result property="studentId" column="student_id"></result>
        <result property="professorId" column="professor_id"></result>
        <result property="username" column="username"></result>
        <result property="password" column="password"></result>
        <result property="phone" column="phone"></result>
        <result property="birthDate" column="birthDate"></result>
        <result property="email" column="email"></result>
        <result property="address" column="address"></result>
        <result property="departmentNumber" column="department_number"></result>
        <collection property="roleDtl" javaType="list" resultMap="roleDtl"></collection>
    </resultMap>

    <resultMap id="roleMst" type="com.koreait.koreaitsugang.entity.RoleMst">
        <result property="roleId" column="role_id"></result>
        <result property="roleName" column="role_name"></result>
    </resultMap>

    <resultMap id="roleDtl" type="com.koreait.koreaitsugang.entity.RoleDtl">
        <result property="roleDtlId" column="role_dtl_id"></result>
        <result property="userId" column="user_id"></result>
        <result property="roleId" column="role_id"></result>
        <collection property="roleMst" resultMap="roleMst"></collection>
    </resultMap>

    <select id="findUserByUsername" parameterType="String" resultMap="userMst">
        select
            um.user_id,
            um.student_id,
            um.professor_id,
            um.username,
            um.password,
            um.phone,
            um.birthDate,
            um.email,
            um.address,
            um.department_number,

            rd.role_id,
            rm.role_name
        from
            user_mst um
            left outer join role_dtl rd on(rd.user_id = um.user_id)
            left outer join role_mst rm on(rm.role_id = rd.role_id)
        where
            um.username = #{username}
    </select>

    <select id="findUserByUserId" parameterType="integer" resultMap="userMst">
        select
            um.user_id,
            um.username,
            um.password,
            um.phone,
            um.name,
            um.birth_date as birthDate,
            um.email,
            um.address,
            um.department_number,

            rd.role_id,
            rm.role_name
        from
            user_mst um
            left outer join role_dtl rd on(rd.user_id = um.user_id)
            left outer join role_mst rm on(rm.role_id = rd.role_id)
        where
            um.user_id = #{userId}
    </select>

    <insert id="saveUser" parameterType="com.koreait.koreaitsugang.entity.UserMst" useGeneratedKeys="true" keyProperty="userId">
        insert into user_mst
        values (0, #{username}, #{password}, #{phone}, #{birthDate}, #{email}, #{address}, #{departmentNumber})
    </insert>

    <insert id="saveRole" parameterType="com.koreait.koreaitsugang.entity.UserMst">
        insert into role_dtl
        values (0, #{userId}, 1)
    </insert>

</mapper>
